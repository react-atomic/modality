{
  "version": "0.2.1",
  "name": "modality-cli-kit",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/react-atomic/modality"
  },
  "homepage": "https://github.com/react-atomic/modality",
  "description": "TODO: description",
  "keywords": ["modality-cli-kit"],
  "author": "Hill <hill@kimo.com>",
  "license": "ISC",
  "git.addAICoAuthor": "off",
  "dependencies": {
    "modality-mcp-kit": "^1.7.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "modality-bun-kit": "^1.3.14"
  },
  "exports": {
    "types": "./dist/types/index.d.ts",
    "require": "./dist/index.js",
    "import": "./dist/index.js"
  },
  "types": "./dist/types/index.d.ts",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "scripts": {
    "build:clean": "find ./dist -name '*.*' | xargs rm -rf",
    "build:types": "bun tsc -p ./",
    "build:src": "bun build src/index.ts --outdir dist --target=node",
    "build": "bun run build:clean && bun run build:types && bun run build:src",
    "test": "npm run build && bun test",
    "prepublishOnly": "npm t"
  },
  "files": ["dist", "package.json", "README.md"]
}
